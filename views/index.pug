extends layout.pug

block content

  article.portfolio-intro

    
    h1 Let's make the web great together!

     
    p This porfolio was built using ExpressJs and the pug Template engine as a way to share a few Javscript projects that I've been working on through TreeHouse FullStack Developer TechDegree Program.


  article.portfolio-project-thumbs
    .projects-box.box.grid

      
      
      each project in projects
        .cell
         a(href=`/projects/${project.id}`)
            img.thumbnail(src= `${project.image_urls[0]}`)
            h5= project.project_name
      

            

